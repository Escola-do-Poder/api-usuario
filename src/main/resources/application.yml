spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    drive-class-name: org.postgresql.Driver
    username: postgres
    password: M#m90012
  jpa:
    show-sql: true # Opcional: mostra os SQLs gerados
    hibernate:
      #ddl-auto: none # 'update', 'create', 'validate', 'none'. 'update' � comum em desenvolvimento
      dialect: org.hibernate.dialect.PostgreSQLDialect # Dialeto para o Hibernate

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true












